// src/reducers/headerReducer.js
const initialState = {
    theme: 'light',
  };
  
  const headerReducer = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export default headerReducer;
  